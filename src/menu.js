export const menuInfo = function(parent) {
    const menuInfo = document.createElement('div');
    menuInfo.id = 'menu-info'
    menuInfo.innerHTML = "fill this later"
    parent.appendChild(menuInfo);
};