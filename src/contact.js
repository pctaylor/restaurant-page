export const contactInfo = function(parent) {
    const contactInfo = document.createElement('div');
    contactInfo.id = 'contact-info'
    contactInfo.innerHTML = "contact me at your butt"
    parent.appendChild(contactInfo);
};